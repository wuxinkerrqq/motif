请聆听这段音乐，识别其中最明显的结构节点，输出 JSON。

{background_section}

注意：
- 只报你**100%确定**听到的那种冲击性节点：drop进入、高潮开始、明显段落切换
- approx_time_sec 是你听到事件**发生那一刻**的时间，不是预期时间
- 宁可只报3个准确的，也不要报6个模糊的
- 每个节点必须填写 emotion 和 visual_scene，这是最重要的输出
